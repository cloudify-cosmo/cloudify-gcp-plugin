test:
  override:
    - tox -e flake8
    - tox -e docs
    - mkdir -p $CIRCLE_TEST_REPORTS/nose
    - tox -e py27 -- --with-xunit --xunit-file=$CIRCLE_TEST_REPORTS/nose/nosetests.xml
    - mv .tox/docs/tmp/html $CIRCLE_ARTIFACTS

